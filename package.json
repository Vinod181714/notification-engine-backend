{
  "name": "notification-prioritization-engine",
  "version": "1.0.0",
  "description": "Backend for notification prioritization engine",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "seed": "node -e \"require('dotenv').config();(async ()=>{await require('./src/config/database')(); await require('./src/utils/seed').createUsersAndRules(); console.log('seed complete'); process.exit(0);})().catch(err=>{console.error(err); process.exit(1);});\""
  },
  "dependencies": {
    "fast-levenshtein": "^2.0.6",
    "express": "^4.18.2",
    "mongoose": "^7.4.2",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "cors": "^2.8.5",
    "helmet": "^7.0.1",
    "express-rate-limit": "^6.9.0",
    "openai": "^4.4.0",
    "socket.io": "^4.7.2",
    "node-cron": "^3.0.2",
    "winston": "^3.9.0",
    "dotenv": "^16.1.4",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}